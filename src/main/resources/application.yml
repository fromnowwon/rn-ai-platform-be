spring:
  application:
    name: ${APP_NAME:RnAiPlatformApplicationBackend}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization:
      indent-output: true
server:
  port: 8080
